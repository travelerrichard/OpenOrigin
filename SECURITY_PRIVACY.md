# Security & Privacy

- `redact` policy removes GPS, device IDs, CreatorTool; optionally coarsens CreateDate.
- Hashes are SHA-256 and recorded in `manifest.json` for tamper-evidence.
- Visual content can still reveal location or identity.
